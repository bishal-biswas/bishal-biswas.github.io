---
interface Props {
    isVerticallySpaced ?: boolean
}

const {isVerticallySpaced = true} = Astro.props;
---
<span class="divider block w-full border-t border-dotted border-gray-500" class:list={[
    isVerticallySpaced && "my-4"
]}></span>